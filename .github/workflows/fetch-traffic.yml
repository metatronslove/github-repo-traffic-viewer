name: GitHub Traffic Data Collector

on:
  schedule:
    - cron: '0 * * * *'  # Hourly run
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch-traffic-data:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pygithub==1.59.0 pyyaml

      - name: Initialize Data Directory
        run: |
          mkdir -p docs/data/repos
          echo "DATA_DIR=docs/data" >> $GITHUB_ENV

      - name: Collect and Merge Traffic Data
        env:
          GH_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}
        run: |
          python - <<EOF
          from github import Github
          import os
          import json
          import time
          from datetime import datetime, timezone

          g = Github(os.environ['GH_TOKEN'])
          data_dir = os.environ['DATA_DIR']
          
          def merge_data(existing, new_items, data_key):
              # Mevcut veri yoksa yeni yapı oluştur
              if not existing or data_key not in existing:
                  existing = {data_key: [], "count": 0, "uniques": 0}
              
              # Mevcut timestamp'leri normalize et ve set oluştur
              existing_timestamps = set()
              for item in existing[data_key]:
                  ts = item['timestamp']
                  if not ts.endswith('Z'):
                      ts = ts + 'Z'
                  existing_timestamps.add(ts)
              
              # Mevcut verileri timestamp'e göre indexle
              existing_by_timestamp = {}
              for item in existing[data_key]:
                  ts_key = item['timestamp']
                  if not ts_key.endswith('Z'):
                      ts_key = ts_key + 'Z'
                  existing_by_timestamp[ts_key] = item
              
              # Yeni verileri ekle/güncelle
              merged = existing[data_key].copy()
              changes_made = False
              
              print(f"    Mevcut: {len(existing_timestamps)} kayıt, Yeni: {len(new_items)} kayıt")
              
              for item in new_items:
                  # Doğru timestamp formatı
                  new_ts = item.timestamp.astimezone(timezone.utc).strftime('%Y-%m-%dT%H:%M:%SZ')
                  
                  if new_ts in existing_timestamps:
                      # Varolan veriyi kontrol et
                      existing_item = existing_by_timestamp.get(new_ts)
                      if existing_item:
                          if item.count > existing_item['count'] or item.uniques > existing_item['uniques']:
                              # Eski veriyi çıkar
                              merged = [x for x in merged if x['timestamp'] != existing_item['timestamp']]
                              # Yeni veriyi ekle
                              merged.append({
                                  "timestamp": new_ts,
                                  "count": item.count,
                                  "uniques": item.uniques
                              })
                              changes_made = True
                              print(f"      ↻ Güncellendi: {new_ts} (count: {item.count}, uniques: {item.uniques})")
                  else:
                      # Yeni timestamp
                      merged.append({
                          "timestamp": new_ts,
                          "count": item.count,
                          "uniques": item.uniques
                      })
                      changes_made = True
                      print(f"      ✚ Eklendi: {new_ts} (count: {item.count}, uniques: {item.uniques})")
              
              if changes_made:
                  # Timestamp'e göre sırala
                  merged.sort(key=lambda x: x['timestamp'])
                  
                  # Toplamları güncelle
                  total_count = sum(item['count'] for item in merged)
                  total_uniques = sum(item['uniques'] for item in merged)
                  
                  print(f"    ✅ Güncellendi: {len(merged)} toplam kayıt")
                  
                  return {
                      data_key: merged,
                      "count": total_count,
                      "uniques": total_uniques
                  }, True
              else:
                  print(f"    ⏺️ Değişiklik yok")
                  return existing, False

          try:
              repos = list(g.get_user().get_repos(type='all'))
              repo_index_path = f"{data_dir}/repo-info.json"
              
              # Mevcut repo index'i oku
              existing_repo_index = []
              if os.path.exists(repo_index_path):
                  with open(repo_index_path, 'r') as f:
                      existing_repo_index = json.load(f)

              # Hızlı erişim için dictionary oluştur
              existing_dict = {repo['name']: repo for repo in existing_repo_index}
              updated_repos = []
              
              stats = {
                  'processed': 0,
                  'errors': 0,
                  'new_data': 0,
                  'updated': 0,
                  'no_change': 0
              }

              for repo in repos:
                  if repo.name.startswith('.'):
                      continue
                      
                  repo_dir = f"{data_dir}/repos/{repo.name}"
                  os.makedirs(repo_dir, exist_ok=True)
                  
                  try:
                      print(f"\n📦 İşleniyor: {repo.name}")
                      repo_changed = False
                      
                      # Views verilerini işle
                      views_file = f"{repo_dir}/views.json"
                      views_data = {}
                      
                      if os.path.exists(views_file):
                          with open(views_file, 'r') as f:
                              views_data = json.load(f)
                          print(f"  👁️ Views dosyası okundu")
                      
                      try:
                          new_views = repo.get_views_traffic()
                          if new_views and hasattr(new_views, 'views') and new_views.views:
                              print(f"  📊 Views API'den {len(new_views.views)} kayıt alındı")
                              merged_views, views_changed = merge_data(views_data, new_views.views, 'views')
                              
                              if views_changed:
                                  with open(views_file, 'w') as f:
                                      json.dump(merged_views, f, indent=2)
                                  repo_changed = True
                      except Exception as e:
                          print(f"  ⚠️ Views hatası: {str(e)}")
                      
                      # Clones verilerini işle
                      clones_file = f"{repo_dir}/clones.json"
                      clones_data = {}
                      
                      if os.path.exists(clones_file):
                          with open(clones_file, 'r') as f:
                              clones_data = json.load(f)
                          print(f"  📋 Clones dosyası okundu")
                      
                      for attempt in range(3):
                          try:
                              new_clones = repo.get_clones_traffic()
                              if new_clones and hasattr(new_clones, 'clones') and new_clones.clones:
                                  print(f"  📊 Clones API'den {len(new_clones.clones)} kayıt alındı")
                                  merged_clones, clones_changed = merge_data(clones_data, new_clones.clones, 'clones')
                                  
                                  if clones_changed:
                                      with open(clones_file, 'w') as f:
                                          json.dump(merged_clones, f, indent=2)
                                      repo_changed = True
                              break
                          except Exception as e:
                              if attempt == 2:
                                  print(f"  ❌ Clones hatası: {str(e)}")
                              else:
                                  time.sleep(2)
                      
                      # Repo index'i güncelle
                      existing_data = existing_dict.get(repo.name, {})
                      new_entry = {
                          "name": repo.name,
                          "full_name": repo.full_name,
                          "private": repo.private,
                          "default_branch": repo.default_branch,
                          "homepage": repo.homepage if repo.homepage else "",
                          "updated_at": datetime.now(timezone.utc).isoformat()
                      }
                      
                      # Mevcut özel alanları koru
                      for key, value in existing_data.items():
                          if key not in new_entry:
                              new_entry[key] = value
                      
                      updated_repos.append(new_entry)
                      stats['processed'] += 1
                      
                      if repo_changed:
                          if repo.name in existing_dict:
                              stats['updated'] += 1
                              print(f"  ✅ Repo güncellendi")
                          else:
                              stats['new_data'] += 1
                              print(f"  ✅ Yeni repo eklendi")
                      else:
                          stats['no_change'] += 1
                          print(f"  ⏺️ Değişiklik yok")

                      time.sleep(1.2)
                      
                  except Exception as e:
                      print(f"  ❌ Hata: {str(e)}")
                      stats['errors'] += 1
                      if repo.name in existing_dict:
                          updated_repos.append(existing_dict[repo.name])

              # Repoları isme göre sırala
              updated_repos.sort(key=lambda x: x['name'])
              
              # Nihai repo index'i yaz
              with open(repo_index_path, 'w') as f:
                  json.dump(updated_repos, f, indent=2)
              
              print(f"\n{'='*50}")
              print(f"✅ İşlem tamamlandı!")
              print(f"📊 İşlenen repo: {stats['processed']}")
              print(f"✨ Yeni repo: {stats['new_data']}")
              print(f"🔄 Güncellenen: {stats['updated']}")
              print(f"⏺️ Değişmeyen: {stats['no_change']}")
              print(f"⚠️ Hata: {stats['errors']}")
              print(f"{'='*50}")

          except Exception as e:
              print(f"🔥 Kritik hata: {str(e)}")
              raise
          EOF

      - name: Commit and Push Changes
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          
          git add docs/data/
          
          if git diff --quiet && git diff --staged --quiet; then
            echo "ℹ️ No changes to commit"
            exit 0
          fi
          
          git commit -m "📈 Update traffic data: $(date -u '+%Y-%m-%d %H:%M UTC')"
          
          MAX_RETRIES=5
          for i in $(seq 1 $MAX_RETRIES); do
            echo "🔄 Push attempt $i of $MAX_RETRIES..."
            
            git pull --rebase origin main && git push origin main && exit 0
            
            if [ $i -eq $MAX_RETRIES ]; then
              echo "❌ All push attempts failed"
              exit 1
            fi
            
            sleep $((2 ** i))
          done
