name: GitHub Traffic Data Collector

on:
  schedule:
    - cron: '0 * * * *'  # Her saat başı
  workflow_dispatch:

jobs:
  collect-traffic-data:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Sadece commit yetkisi
      pull-requests: read
      metadata: read

    steps:
      # 1. Repoyu klonla
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2. Trafik verilerini topla
      - name: Fetch traffic data
        run: |
          mkdir -p docs/data
          
          # Views verisi
          curl -H "Accept: application/vnd.github+json" \
               -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
               https://api.github.com/repos/${{ github.repository }}/traffic/views > docs/data/views.json
          
          # Clones verisi
          curl -H "Accept: application/vnd.github+json" \
               -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
               https://api.github.com/repos/${{ github.repository }}/traffic/clones > docs/data/clones.json
          
          # Repo bilgisi
          curl -s -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
               https://api.github.com/repos/${{ github.repository }} > docs/data/repo-info.json

      # 3. Verileri commitle
      - name: Commit collected data
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add docs/data/
          git commit -m "📈 Traffic data update [$(date +'%Y-%m-%d %H:%M')]" || echo "No changes to commit"
          git push